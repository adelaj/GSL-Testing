language: c

git:
    depth: 1

matrix:
    include:
      - os: linux
        dist: bionic
        addons:            
            apt:
                sources:
                  - sourceline: 'deb https://apt.kitware.com/ubuntu/ bionic main'
                    key_url: https://apt.kitware.com/keys/kitware-archive-latest.asc
                packages:
                  - cmake
                  - gcc-multilib
                  - llvm-dev
                  - binutils

script:
  - make -f RegionsMT/Makefile PREFIX="." MATRIX="`echo {gcc,clang}:{x86_64,i386}:{Debug,Release}`" -j test\(gsl\)

